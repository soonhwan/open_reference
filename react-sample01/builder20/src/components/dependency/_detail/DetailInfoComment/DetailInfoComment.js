import React, { Fragment } from 'react';

const DetailInfoComment = (props) => {
  return (
    <div>
      상품상세 댓글 컴포넌트 퍼블 필요
      {/* <ul>
        {props.commentList.map((v, i) => <li key={v.channelId}>{v.commentDscr}</li>)}
      </ul> */}
    </div>
  )
}

export default DetailInfoComment;